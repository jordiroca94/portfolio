import React from "react";
import Container from "./Container";

const HumanRights = () => {
  return (
    <Container id="humanRights" className="bg-white h-screen">
      HumanRights
    </Container>
  );
};

export default HumanRights;
